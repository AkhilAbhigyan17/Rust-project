//! Placeholder integration test module. Real integration tests require a running
//! Postgres instance — spin one up with `docker compose up postgres` and set
//! `DATABASE_URL` before running `cargo test --test auth_test -- --ignored`.
#[test]
#[ignore]
fn login_flow_integration() {
    // See docs/testing.md for the full harness.
    assert!(true);
}
